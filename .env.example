NEXT_PUBLIC_SITE_URL=https://marianosolle.com
PORTFOLIO_PLUS_PASSWORD_HASH=

# Analytics — GTM is the single entry point; GA4/Clarity are configured inside GTM
NEXT_PUBLIC_GTM_ID=
# VERCEL_ENV is set automatically by Vercel (values: production | preview | development).
# GTM only loads when VERCEL_ENV=production. No action needed — do not set this manually.

// Navbar scrolled height (py-[12px] * 2 + ~24px line height ≈ 48px).
// Used as the baseline top offset for sticky stacking cards.
export const NAV_HEIGHT = 48

// Vertical offset between stacked cards. Each subsequent card sticks
// this many pixels below the previous one, revealing a thin strip.
export const STACK_OFFSET = 12
